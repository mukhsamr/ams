$(document).ready(function () {
    // Clock
    // setInterval(() => {
    //     const time = new Date;
    //     let h = time.getHours() + '';
    //     let m = time.getMinutes() + '';
    //     let s = time.getSeconds() + '';
    //     let jam = (h.length < 2) ? '0' + h : h;
    //     let mnt = (m.length < 2) ? '0' + m : m;
    //     let dtk = (s.length < 2) ? '0' + s : s;
    //     const waktu = `${jam}:${mnt}:${dtk}`;
    //     $('#clock').text(waktu);

    // }, 1000);
});